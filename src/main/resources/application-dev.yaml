spring:
  # Development Database Configuration (Cloud SQL - hss_dev database)
  datasource:
    url: jdbc:postgresql:///hss_dev?cloudSqlInstance=${GCP_SQL_INSTANCE_CONNECTION_NAME}&socketFactory=com.google.cloud.sql.PostgresSocketFactory
    username: ${DB_USERNAME:hss_user}
    password: ${DB_PASSWORD:hss_password}
    driver-class-name: org.postgresql.Driver
  
  # JPA Configuration for Development
  jpa:
    hibernate:
      ddl-auto: update  # Allow schema updates in dev
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          time_zone: UTC
  
  # GCP Configuration for Development
  cloud:
    gcp:
      sql:
        enabled: true  # Use Cloud SQL in dev
        database-name: hss_dev
        instance-connection-name: ${GCP_SQL_INSTANCE_CONNECTION_NAME:}
      storage:
        bucket-name: ${GCP_STORAGE_BUCKET:hss-dev-storage-bucket}
        project-id: ${GCP_PROJECT_ID:hss-project-id}
  
  # Security Configuration for Development
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${JWT_ISSUER_URI:https://securetoken.google.com/hss-project-id}

# Development specific logging
logging:
  level:
    com.hss.hss_backend: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.springframework.web: DEBUG
